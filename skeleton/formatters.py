# 맵기 단계(1~5)를 별 기호 문자열로 변환한다
# 예: 3 -> "★★★☆☆"
def spicy_stars(level):
    # TODO: level 개수만큼 ★을, 나머지는 ☆을 붙여서 반환하라
    #       ★의 개수: level / ☆의 개수: 5 - level
    #       힌트: "★" * 3 은 "★★★" 이 된다
    pass


# 가격 최솟값과 최댓값을 "X,XXX원 ~ X,XXX원" 형식으로 변환한다
# 예: (8000, 12000) -> "8,000원 ~ 12,000원"
def format_price(price_min, price_max):
    # TODO: price_min과 price_max를 "8,000원 ~ 12,000원" 형식으로 반환하라
    #       힌트: f"{숫자:,}" 를 사용하면 1000 단위로 콤마가 자동으로 붙는다
    pass
